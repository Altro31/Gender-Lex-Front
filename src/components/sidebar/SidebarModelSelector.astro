---
import { getCurrentModel } from "@/lib/domain"
import { availableModelsName } from "@/services/ai/models"
import Combobox from "~ui/combobox"

const modelName = getCurrentModel(Astro)
---

<Combobox options={availableModelsName} defaultValue={modelName} client:load />
